# ==============================================
# 배터리 검사 시스템 환경 변수
# ==============================================
# 사용법: 이 파일을 복사하여 .env 파일로 저장
#         cp .env.example .env
# ==============================================

# ===== API 키 (필수) =====
# Google Gemini API 키 (Gemini VLM 사용 시 필요)
GEMINI_API_KEY=your_gemini_api_key_here

# OpenAI API 키 (GPT 관련 기능 사용 시)
OPENAI_API_KEY=your_openai_api_key_here


# ===== VLM (Vision Language Model) 설정 =====
# 기본 VLM 모델 선택 (qwen2vl | gemini)
VLM_DEFAULT_MODEL=qwen2vl

# Qwen2-VL 모델 크기 (2b | 7b)
VLM_MODEL_SIZE=2b

# Gemini 모델명
GEMINI_MODEL_NAME=gemini-2.0-flash


# ===== VLG (Visual Language Grounding) 설정 =====
# 기본 VLG 모델 선택 (groundingdino | yoloworld)
VLG_DEFAULT_MODEL=groundingdino

# 객체 탐지 임계값 (0.0 ~ 1.0)
VLG_BOX_THRESHOLD=0.3
VLG_TEXT_THRESHOLD=0.25


# ===== CNN 모델 설정 =====
# 분류 클래스 수 (기본: 5)
CNN_NUM_CLASSES=5

# 입력 이미지 크기
CNN_IMAGE_SIZE=512

# 모델 체크포인트 경로 (optional)
# CNN_CHECKPOINT_PATH=checkpoints/ct_cnn/best_model.pt
# AE_CHECKPOINT_PATH=checkpoints/rgb_ae/best_model.pt


# ===== 데이터 경로 =====
# 데이터셋 루트 경로 (optional)
# DATA_ROOT=/path/to/battery-inspection-data


# ===== 웹앱 설정 =====
# Streamlit 포트
WEBAPP_PORT=8501

# 디버그 모드 (true | false)
WEBAPP_DEBUG=false


# ===== 기존 설정 (참고용) =====
# Python 가상환경 경로
VIRTUAL_ENV=./venv

# 서버 설정 (Node.js 백엔드용)
# PORT=3000
# NODE_ENV=development
